class Solution:
    # @return an integer
    def minDistance(self, word1, word2):
        lenA = len(word1)
        lenB = len(word2)
        if (lenA ==0) and (lenB == 0):
            return 0
        return 0
        